/**
 * get the register.html from Views
 * In signUp case if register clicked then call registerUser and userProfile function in user.js/Models
 * In Login case if login button entered then call loginUser function in user.js/Models. Then direct to home.html
 * In  case of Forgot password,direct to forgotPassword.html then call validateEmail,generateCode and validateCode function in user.js/Models
 * 
 *  */ 
