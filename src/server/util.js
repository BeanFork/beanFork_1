generateMail = function(email,code){
    //using the nodemailer in npm send the verification code
}
var passwordConfirmation = function(password, confirmPassword){

    
}
var mailidFormat = function(emailid){
    // Check Mail ID format (For eg : user@gmail.com)
}